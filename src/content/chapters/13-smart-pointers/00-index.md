---
title: "智能指针"
description: "深入理解 Rust 的内存管理机制，掌握 Box、Rc、RefCell 等核心智能指针的使用场景与底层原理。"
difficulty: beginner
estimatedTime: 5
keywords: ["智能指针", "Box", "Rc", "RefCell", "Deref", "Drop", "所有权", "堆内存"]
---

指针（Pointer）是一个包含内存地址的变量。而 **智能指针（Smart Pointers）** 是一类特殊的结构体，它们不仅表现得像指针，还拥有额外的元数据和功能。

在 Rust 中，普通的引用（`&`）只负责借用数据。而智能指针通常**拥有**它们所指向的数据。

智能指针的主要使用场景：

| 场景 | 推荐类型 |
|------|----------|
| 将数据分配到堆上，或定义递归类型 | `Box<T>` |
| 单线程下多处共享同一份只读数据 | `Rc<T>` |
| 需要在"不可变"的外壳下修改内部数据 | `RefCell<T>` |
| 单线程下多处共享且需要修改数据 | `Rc<RefCell<T>>` |
| 多线程下共享数据| `Arc<T>` / `Arc<Mutex<T>>` （后续并发章节讲解）|

## 本章学习目标

通过本章的学习，你将能够：
1. 理解「间接引用」的概念及堆内存分配原理。
2. 掌握 `Deref` 和 `Drop` 这两个让「普通结构体」变身「智能指针」的基础 Trait。
3. 区分单线程下的共享所有权（`Rc`）与内部可变性（`RefCell`）。
4. 初步了解多线程下的安全共享（`Arc` 与 `Mutex`）。

## 章节目录

1. **[Box\<T\>：指向堆内存](01-box)** - 解决递归类型问题，实现简单的堆分配。
2. **[Deref 与 Drop：指针的魔法](02-deref-drop)** - 学习如何自定义指针行为以及自动资源清理。
3. **[Rc\<T\>：引用计数指针](03-rc)** - 实现单线程下的「多重所有权」。
4. **[RefCell\<T\>：内部可变性](04-refcell)** - 在不可变引用的外壳下修改内部数据。
