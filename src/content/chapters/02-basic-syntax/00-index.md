---
title: "基础语法"
description: "从注释、变量、数据类型到控制流、函数、宏，系统掌握 Rust 程序的基本构成要素。"
difficulty: beginner
estimatedTime: 5
keywords: ["基础语法", "变量", "数据类型", "控制流", "函数", "宏"]
---

学一门语言，第一步永远是同一件事：弄清楚它的基本构成要素——变量怎么声明，类型系统什么样，怎么写条件分支和循环，怎么定义函数。

这些东西在各门语言里大同小异，但 Rust 有几处独特的地方值得注意。比如变量默认不可变，比如 `if` 是表达式而不是语句，比如函数的返回值是最后一个表达式的值而不是 `return`。这些设计选择背后都有原因，理解了原因，规则就不难记住。

本章把这些基础语法挨个讲清楚，外加属性和宏两个 Rust 特有的概念。

## 一段代码，看懂本章要学什么

下面这段代码虽然不长，但用到了本章涵盖的大部分内容——变量、数据类型、函数、控制流、循环和格式化输出：

```rust runnable
// 根据分数判断等级（函数 + 控制流）
fn grade(score: u32) -> &'static str {
    if score >= 90 {
        "优秀"
    } else if score >= 80 {
        "良好"
    } else if score >= 60 {
        "及格"
    } else {
        "不及格"
    }
}

fn main() {
    // 变量与数组
    let scores = [95, 82, 61, 45];

    // for 循环遍历
    for score in scores {
        // 格式化输出（println! 宏）
        println!("分数 {:>3}：{}", score, grade(score));
    }
}
```

读完本章，你会知道这段代码里每一行是什么意思，也能自己写出类似的程序。

> 如果你是有经验的程序员，前几节可以快速浏览——重点关注和你熟悉语言的**差异**，而不是从零理解每个概念。

## 本章目录

| 文章 | 主要内容 |
| --- | --- |
| [注释](./01-comments) | 行注释、块注释与文档注释的写法和用途 |
| [格式化输出](./02-formatted-output) | `println!` 宏的各种格式化占位符，调试打印与自定义格式 |
| [数据类型](./03-data-types) | 整数、浮点、布尔、字符等基础类型，以及元组和数组 |
| [变量与可变性](./04-variables) | `let` 绑定、默认不可变、`mut`、常量与遮蔽（shadowing） |
| [控制流](./05-control-flow) | `if` 表达式、`loop`、`while`、`for` 循环的语法与特性 |
| [函数](./06-functions) | 函数定义、参数、返回值，以及表达式与语句的区别 |
| [属性](./07-attributes) | `#[...]` 属性的作用：控制编译行为、附加元数据 |
| [宏](./08-macros) | Rust 宏的基本概念，常用内置宏一览 |
| [综合练习](./09-practice) | 用本章所学语法完成编程练习，巩固基础 |
